/* @Flow */
//TOOD: make a regex to check on only special characters and regular characters + numbers (no e trema and so on these are not allowed).
function isValidUsername(username: string): boolean {
  if(username.trim() === "") return false;

}

function isSimilarPassword(password: string,conformationPassword: string): boolean {

}